---
category: concern
triggers:
  - ".claude-plugin/plugin.json"
  - ".claude-plugin/marketplace.json"
---

# Version Sync

- [ ] `plugin.json` and `marketplace.json` version fields must match — bump both in a single commit
