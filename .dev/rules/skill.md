---
category: domain
triggers:
  - "skills/*/SKILL.md"
  - "skills/*/references/*.md"
depends_on:
  - docs
---

# Skill Changes

- [ ] If a new skill is added, `CLAUDE.md` "Recent Changes" section mentions it
- [ ] If a new skill is added, `README.md` skill list is updated
- [ ] If skill `description` in frontmatter changed, verify trigger phrases still match actual usage
- [ ] If `references/` files are added or modified, SKILL.md body references them correctly
