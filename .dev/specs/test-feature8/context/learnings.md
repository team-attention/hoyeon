## TODO 1
- This project uses template literals for dynamic classNames (not classnames library)
- Component follows simple functional component pattern with destructured props
- CSS uses kebab-case class names (button-primary, button-secondary)
- ESLint is configured and runs via npm run lint
