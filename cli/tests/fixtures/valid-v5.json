{
  "meta": {
    "name": "test-project",
    "goal": "Test the hoyeon-cli spec validation with a complete v5 fixture",
    "type": "dev",
    "schema_version": "v5",
    "created_at": "2026-01-01T00:00:00.000Z"
  },
  "context": {
    "request": "Add schema validation tests",
    "interview": [
      {
        "topic": "Test coverage",
        "decision": "Cover validate, merge, and plan commands"
      }
    ]
  },
  "requirements": [
    {
      "id": "R1",
      "behavior": "CLI validates spec.json against v5 schema",
      "priority": 1,
      "source": { "type": "goal" },
      "scenarios": [
        {
          "id": "R1-S1",
          "given": "A valid v5 spec.json file",
          "when": "hoyeon-cli spec validate is run",
          "then": "Exit code 0 and no errors printed",
          "verified_by": "machine",
          "execution_env": "host",
          "verify": {
            "type": "command",
            "run": "hoyeon-cli spec validate ./spec.json",
            "expect": { "exit_code": 0 }
          }
        },
        {
          "id": "R1-S2",
          "given": "An invalid spec.json missing meta",
          "when": "hoyeon-cli spec validate is run",
          "then": "Exit code 1 and validation errors printed",
          "verified_by": "machine",
          "execution_env": "host",
          "verify": {
            "type": "command",
            "run": "hoyeon-cli spec validate ./invalid.json",
            "expect": { "exit_code": 1 }
          }
        }
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "action": "Create test infrastructure",
      "type": "work",
      "status": "pending",
      "risk": "low",
      "file_scope": ["cli/tests/"],
      "depends_on": [],
      "steps": [
        "Create fixtures directory",
        "Create helper utilities",
        "Add test script to package.json"
      ],
      "acceptance_criteria": {
        "scenarios": ["R1-S1"],
        "checks": [
          { "type": "build", "run": "cd cli && npm test" }
        ]
      }
    }
  ]
}
