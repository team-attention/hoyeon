{
  "meta": {
    "name": "bad-category-test",
    "goal": "Test scenario with invalid category field",
    "schema_version": "v5"
  },
  "requirements": [
    {
      "id": "R1",
      "behavior": "CLI rejects invalid category values",
      "priority": 1,
      "scenarios": [
        {
          "id": "R1-S1",
          "category": "INVALID",
          "given": "A spec with invalid category INVALID",
          "when": "spec validate is run",
          "then": "Validation fails with enum violation",
          "verified_by": "machine",
          "execution_env": "host",
          "verify": {
            "type": "command",
            "run": "hoyeon-cli spec validate ./spec.json",
            "expect": { "exit_code": 0 }
          }
        }
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "action": "Test bad category field",
      "type": "work"
    }
  ]
}
