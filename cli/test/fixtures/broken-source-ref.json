{
  "meta": {
    "name": "broken-source-ref-test",
    "goal": "Test source.ref integrity check detects non-existent decision refs",
    "schema_version": "v5"
  },
  "decisions": [
    {
      "id": "D1",
      "title": "Use JSON schema validation",
      "rationale": "Provides structured validation with clear error messages"
    }
  ],
  "requirements": [
    {
      "id": "R1",
      "behavior": "CLI validates source.ref against decisions",
      "priority": 1,
      "source": {
        "type": "decision",
        "ref": "D-NONEXISTENT"
      },
      "scenarios": [
        {
          "id": "R1-S1",
          "given": "A spec with source.ref pointing to non-existent decision",
          "when": "spec check is run",
          "then": "Exits 1 with error message naming broken ref",
          "verified_by": "machine",
          "execution_env": "host",
          "verify": {
            "type": "command",
            "run": "node dist/cli.js spec check test/fixtures/broken-source-ref.json",
            "expect": { "exit_code": 1 }
          }
        }
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "action": "Implement source.ref check",
      "type": "work",
      "status": "pending",
      "acceptance_criteria": {
        "scenarios": ["R1-S1"],
        "checks": []
      }
    }
  ]
}
