{
  "meta": {
    "name": "valid-source-ref-test",
    "goal": "Test source.ref referential integrity passes when all refs are valid",
    "schema_version": "v5"
  },
  "decisions": [
    {
      "id": "D1",
      "title": "Use JSON schema validation",
      "rationale": "Provides structured validation with clear error messages"
    }
  ],
  "requirements": [
    {
      "id": "R1",
      "behavior": "CLI validates source.ref against decisions",
      "priority": 1,
      "source": {
        "type": "decision",
        "ref": "D1"
      },
      "scenarios": [
        {
          "id": "R1-S1",
          "given": "A valid spec with source.ref pointing to D1",
          "when": "spec check is run",
          "then": "Exits 0",
          "verified_by": "machine",
          "execution_env": "host",
          "verify": {
            "type": "command",
            "run": "node dist/cli.js spec check test/fixtures/valid-source-ref.json",
            "expect": { "exit_code": 0 }
          }
        }
      ]
    }
  ],
  "tasks": [
    {
      "id": "T1",
      "action": "Implement source.ref check",
      "type": "work",
      "status": "pending",
      "acceptance_criteria": {
        "scenarios": ["R1-S1"],
        "checks": []
      }
    }
  ]
}
